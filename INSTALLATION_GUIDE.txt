================================================================================
                        Dev Debug Helper 설치 및 사용 가이드
================================================================================

React 개발 시 요소의 className, 파일 경로, 컴포넌트명을 쉽게 복사할 수 있는
디버그 도구입니다.

================================================================================
1. 기능 소개
================================================================================

[Ctrl + 마우스 이동]
  - 마우스가 올라간 요소의 정보를 툴팁으로 미리보기
  - 파일 경로, 라인 번호, 컴포넌트명, className 표시

[Ctrl + 우클릭]
  - 요소 정보를 클립보드에 복사
  - 복사 형식:
    // src/pages/UcellSelect/CellSelectionScreen.tsx:264
    // Component: CellSelectionScreen
    className="h-screen bg-gradient-to-br from-gray-900..."

================================================================================
2. 설치 방법
================================================================================

------------------------------------------------------------------------------
방법 A: 폴더 복사 (가장 간단)
------------------------------------------------------------------------------

1) dev-debug-helper 폴더를 프로젝트의 src 폴더 안에 복사

2) 프로젝트의 src/index.tsx (또는 main.tsx)에 추가:

   // src/index.tsx
   import { initDevDebugHelper } from './dev-debug-helper/src';

   // React 앱 렌더링 전에 호출
   initDevDebugHelper();

------------------------------------------------------------------------------
방법 B: npm 로컬 패키지로 설치
------------------------------------------------------------------------------

1) dev-debug-helper 폴더에서 빌드:

   cd dev-debug-helper
   npm install
   npm run build

2) 프로젝트에서 로컬 패키지로 설치:

   cd your-project
   npm install ../dev-debug-helper

3) 프로젝트에서 import:

   import { initDevDebugHelper } from 'dev-debug-helper';
   initDevDebugHelper();

------------------------------------------------------------------------------
방법 C: npm에 배포 후 설치
------------------------------------------------------------------------------

1) dev-debug-helper 폴더에서:

   npm login
   npm publish

2) 프로젝트에서:

   npm install dev-debug-helper

3) 사용:

   import { initDevDebugHelper } from 'dev-debug-helper';
   initDevDebugHelper();

================================================================================
3. 사용 방법
================================================================================

------------------------------------------------------------------------------
기본 사용
------------------------------------------------------------------------------

// src/index.tsx 또는 main.tsx
import { initDevDebugHelper } from './dev-debug-helper/src';

// 앱 시작 시 한 번만 호출
initDevDebugHelper();

// React 앱 렌더링
ReactDOM.render(<App />, document.getElementById('root'));

------------------------------------------------------------------------------
커스텀 설정
------------------------------------------------------------------------------

initDevDebugHelper({
  // 로컬 스토리지 저장 키 (기본값: 'devDebugEnabled')
  storageKey: 'myAppDebugEnabled',

  // 개발 환경에서만 실행 (기본값: true)
  devOnly: true,

  // 허용할 호스트 목록 (기본값: ['localhost', '127.0.0.1'])
  allowedHosts: ['localhost', '127.0.0.1', 'dev.myapp.com'],

  // 복사 텍스트 포맷 커스터마이징
  formatCopyText: (info) => {
    return `File: ${info.filePath}\nClass: ${info.className}`;
  },

  // 툴팁 포맷 커스터마이징
  formatTooltip: (info) => {
    return `📁 ${info.filePath}\n🏷️ ${info.className}`;
  },

  // 테마 색상
  theme: {
    primary: '#00ff88',      // 알림 배경색
    background: '#1a1a2e',   // 툴팁 배경색
    text: '#00ff88',         // 텍스트 색상
    border: '#00ff88',       // 테두리 색상
  },
});

================================================================================
4. 브라우저 콘솔 명령어
================================================================================

브라우저 개발자 도구(F12) 콘솔에서 직접 제어할 수 있습니다:

  toggleDevDebug()     // 디버그 모드 토글 (켜기/끄기)
  enableDevDebug()     // 디버그 모드 활성화
  disableDevDebug()    // 디버그 모드 비활성화

================================================================================
5. API 레퍼런스
================================================================================

------------------------------------------------------------------------------
함수 목록
------------------------------------------------------------------------------

| 함수                          | 설명                              |
|-------------------------------|-----------------------------------|
| initDevDebugHelper(config?)   | 초기화 (앱 시작 시 한 번만 호출)  |
| destroyDevDebugHelper()       | 완전히 제거                       |
| setDebugEnabled(enabled)      | 활성화/비활성화 설정              |
| isDebugEnabled()              | 현재 활성화 상태 확인             |
| toggleDebugMode()             | 토글                              |

------------------------------------------------------------------------------
ElementInfo 타입
------------------------------------------------------------------------------

interface ElementInfo {
  className: string;         // 요소의 className
  filePath: string | null;   // 소스 파일 경로 (예: src/pages/Home.tsx)
  lineNumber: string | null; // 라인 번호
  componentName: string | null; // React 컴포넌트 이름
  tagName: string;           // HTML 태그 이름
}

------------------------------------------------------------------------------
DevDebugConfig 타입
------------------------------------------------------------------------------

interface DevDebugConfig {
  storageKey?: string;                    // 로컬 스토리지 키
  devOnly?: boolean;                      // 개발 환경에서만 실행 여부
  allowedHosts?: string[];                // 허용 호스트 목록
  formatCopyText?: (info: ElementInfo) => string;  // 복사 포맷
  formatTooltip?: (info: ElementInfo) => string;   // 툴팁 포맷
  theme?: {
    primary?: string;
    background?: string;
    text?: string;
    border?: string;
  };
}

================================================================================
6. 주의사항
================================================================================

1) React 개발 모드에서만 파일 경로 추출 가능
   - 프로덕션 빌드에서는 _debugSource 정보가 제거됨
   - 파일 경로, 라인 번호, 컴포넌트명은 개발 모드에서만 표시

2) localhost에서만 동작 (기본 설정)
   - 기본적으로 localhost와 127.0.0.1에서만 활성화
   - allowedHosts 옵션으로 추가 호스트 허용 가능

3) 성능 영향 없음
   - 개발 환경에서만 동작
   - 프로덕션에서는 자동으로 비활성화

4) TypeScript 프로젝트에서 사용 시
   - 타입 정의가 포함되어 있음
   - 별도의 @types 패키지 불필요

================================================================================
7. 문제 해결
================================================================================

Q: 파일 경로가 표시되지 않아요
A: React 프로덕션 빌드에서는 파일 정보가 제거됩니다.
   npm start로 개발 모드에서 실행하세요.

Q: Ctrl+우클릭이 작동하지 않아요
A: 1) 콘솔에서 enableDevDebug()를 실행하여 활성화
   2) 브라우저 확장 프로그램이 우클릭을 가로채는지 확인

Q: 툴팁이 나타나지 않아요
A: 1) Ctrl 키를 누른 상태로 마우스를 이동
   2) className이 있는 요소 위에서만 동작

Q: 다른 호스트에서 사용하고 싶어요
A: initDevDebugHelper({
     allowedHosts: ['localhost', '127.0.0.1', 'your-host.com']
   });

================================================================================
8. 파일 구조
================================================================================

dev-debug-helper/
├── src/
│   └── index.ts          # 메인 소스 코드
├── dist/                 # 빌드 결과물 (npm run build 후 생성)
│   ├── index.js
│   ├── index.d.ts
│   └── index.js.map
├── package.json          # npm 패키지 설정
├── tsconfig.json         # TypeScript 설정
├── README.md             # 마크다운 문서
└── INSTALLATION_GUIDE.txt  # 이 파일

================================================================================
                              버전: 1.0.0
                           라이선스: MIT
================================================================================
